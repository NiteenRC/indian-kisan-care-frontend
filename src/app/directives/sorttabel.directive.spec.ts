import { SorttabelDirective } from './sorttabel.directive';

describe('SorttabelDirective', () => {
  it('should create an instance', () => {
    const directive = new SorttabelDirective();
    expect(directive).toBeTruthy();
  });
});
